# 디자인 및 지도 연동 구현 노트

## 범위

- 시민 신고 화면의 설명용 지도 영역을 실제 지도와 신고 좌표 표기로 교체한다.
- 직원 접수·배정 화면의 정적 수거구역 그림을 실제 지도와 신고 위치 표기로 교체한다.
- 기존 `POST /api/reports`, `GET /api/reports` 및 좌표 저장 구조는 유지한다.

## 선택

- Leaflet 1.9.4와 OpenStreetMap 타일을 CDN으로 불러온다. 별도 API 키 없이 동작하며, 운영 전에는 시의 지도 공급자·타일 사용량 정책을 확정해야 한다.
- 주소 검색은 기존 카카오맵 새 창 확인 기능을 유지한다. 주소를 좌표로 변환하는 서버 기능은 API 키·이용약관 결정이 필요하므로 이번 범위에서 추가하지 않는다.
- 현재 위치는 사용자가 버튼을 눌러 권한을 허용한 경우에만 지도에 표시하고 신고에 저장한다. 지속 추적은 추가하지 않는다.

## 확인 항목

- 시민 화면에서 현재 위치 확인 시 지도 중심과 핀이 함께 갱신되는지 확인한다.
- 직원 화면에서 좌표가 있는 신고가 지도 핀으로 표시되고, 핀·목록 선택이 서로 동기화되는지 확인한다.
- 좌표가 없는 신고는 지도에 표시하지 않고 기존 검증 흐름으로 남긴다.

## 품목 및 배출 안내 확장

- 대형폐기물 수수료 예시는 가구·생활·레저 품목에만 사용한다. 실제 수수료는 제천시 확정 수수료표와 대조가 필요하다.
- 냉장고, 세탁기, 에어컨, TV, 전자레인지 등 방문수거 대상 폐가전은 대형폐기물 신고 목록에 넣지 않고 `15990903.or.kr` 또는 `1599-0903` 예약 안내를 제공한다.
- 재활용품은 대형폐기물 신고 대상이 아니므로 품목별 세척·분리·전용 수거함 배출 방법만 안내한다. 단지별 수거 품목과 요일은 현장 기준을 우선한다.
